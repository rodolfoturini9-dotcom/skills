# Opcional: a chave pode ser informada na própria interface (Configurações) em vez daqui.
ANTHROPIC_API_KEY=
# Modelo padrão; a interface permite trocar por solicitação.
ANTHROPIC_MODEL=claude-opus-5
# Recomendado quando acessado por outras máquinas; use segredo aleatório forte.
COPILOT_ACCESS_TOKEN=
# Opcional: somente leitura de MedicationRequest FHIR R4.
FHIR_BASE_URL=
FHIR_BEARER_TOKEN=
